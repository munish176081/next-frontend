import CalendarCallbackPage from '../../../../_components/calendar/oauth-callback';

export default function Page() {
  return <CalendarCallbackPage />;
}

export const metadata = {
  title: 'Calendar Authorization - Pups4Sale',
  description: 'Completing Google Calendar authorization',
};
